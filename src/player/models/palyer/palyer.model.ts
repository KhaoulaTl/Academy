/* eslint-disable prettier/prettier */
export interface IPlayer {
    _id?: number;
    firstName: string;
    lastName: string;
    birthDate: Date;
    parentId: string;
    coachId: string;
    categoryId: string;
    skillLevel: string;
}
