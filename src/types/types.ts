export interface ParentType {
    _id: string;
    firstName: string;
    lastName: string;
  }
  
  export interface CoachType {
    _id: string;
    firstName: string;
    lastName: string;
  }
  
  export interface CategoryType {
    _id: string;
    name: string;
  }